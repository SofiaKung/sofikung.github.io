---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';
import { compareProjectImportance, hasDraftTag } from '../utils/content';

const projectEntries = (await getCollection('projects')).filter((entry) => !hasDraftTag(entry));
const sortedProjects = [...projectEntries].sort(compareProjectImportance);
const featured = sortedProjects
  .filter((p) => (p.data.project?.tags || []).some((t) => String(t).toLowerCase() === 'featured'))
  .slice(0, 2);
const projects = featured.length ? featured : sortedProjects.slice(0, 2);

const posts = (await getCollection('posts'))
  .filter((entry) => !hasDraftTag(entry))
  .sort((a, b) => {
    const aDate = a.data.date ? new Date(a.data.date).getTime() : 0;
    const bDate = b.data.date ? new Date(b.data.date).getTime() : 0;
    return bDate - aDate;
  })
  .slice(0, 2);
---
<BaseLayout>
  <main>
    <!-- Hero Section -->
    <section class="hero" id="home">
      <div class="container">
        <div class="hero-layout">
          <div class="hero-content">
            <h1 class="hero-name">Sofia Kung</h1>
            <p class="hero-typing">
              I am <span id="typed-text"></span>
            </p>
 
            <div class="hero-description">
              <p>
                I'm a senior data analyst turned AI builder. After six years working across trust & safety, risk strategy and consulting at Binance, Gojek, and PwC — being the person who identifies risk and deploys solutions before damage compounds — I took a deliberate step back to ask what was worth building next.
              </p>
              <p>
                I spent the past months going deep: AI automation, web apps, agents, taking ideas from zero to product. I've always wanted to build and now I'm focused on where it all converges: AI adoption that drives business outcomes, underpinned by the observability and risk thinking that makes it sustainable.
              </p>
            </div>
          </div>

          <!-- Photo deck hidden for now -->
          <!-- <div class="hero-graphic">
            <div class="graphic-element">
              <div class="photo-deck" id="photo-deck" aria-label="Photo gallery">
                <div class="deck-card"><img src="/assets/photo-1.JPG" alt="Photo 1" loading="lazy" /></div>
                <div class="deck-card"><img src="/assets/photo-2.JPG" alt="Photo 2" loading="lazy" /></div>
                <div class="deck-card"><img src="/assets/photo-3.JPG" alt="Photo 3" loading="lazy" /></div>
                <div class="deck-card"><img src="/assets/photo-4.JPG" alt="Photo 4" loading="lazy" /></div>
                <button id="deck-again" class="btn-again" type="button">
                  <i class="fa-solid fa-rotate-right" aria-hidden="true"></i>
                  <span>Again</span>
                </button>
              </div>
            </div>
          </div> -->
        </div>
      </div>
    </section>

    <section class="projects" id="projects">
      <div class="container">
        <div class="section-header">
          <h2 class="section-title">Featured Projects</h2>
          <a class="more-link" href="/projects/" aria-label="View all projects">More projects →</a>
        </div>
        <div class="project-grid">
          {projects.map(({ slug, data }) => {
            const tags = data.project?.tags ?? [];
            const external = data.external_link;
            const href = external ? external : `/project/${slug}`;
            const externalAttrs = external ? { target: '_blank', rel: 'noopener' } : {};
            const cover = data.hero?.image ?? data.image;
            const alt = data.hero?.alt ?? data.alt ?? data.title;
            return (
              <article class="project-card">
                <a class="project-link" href={href} {...externalAttrs}>
                  <div class="project-media">
                    {cover && <img src={`/${cover}`} alt={alt} loading="lazy" />}
                  </div>
                  <div class="project-body">
                    <div class="project-meta">
                      {tags.map(t => <span class="project-tag">{t}</span>)}
                    </div>
                    <h3 class="project-title">{data.title}</h3>
                    {data.description && <p class="project-desc">{data.description}</p>}
                  </div>
                </a>
              </article>
            );
          })}
        </div>
      </div>
    </section>

    <!-- Blog section hidden for now
    <section class="posts" id="posts">
      <div class="container">
        <div class="section-header">
          <h2 class="section-title">Recent Posts</h2>
          <a class="more-link" href="/blog/" aria-label="View all posts">More posts →</a>
        </div>
        <div class="post-list">
          {posts.map(({ slug, data }) => (
            <article class="post-card">
              <a class="post-link" href={`/post/${slug}`}>
                <header class="post-header">
                  <h3 class="post-title">{data.title}</h3>
                  {data.date && <time class="post-date" datetime={data.date}>{data.date_pretty ?? data.date}</time>}
                </header>
                <p class="post-excerpt">{data.description ?? ''}</p>
                <div class="post-tags">
                  {(data.tags ?? []).map(t => <span class="post-tag">{t}</span>)}
                </div>
              </a>
            </article>
          ))}
        </div>
      </div>
    </section>
    -->

    <!-- Experience Section -->
    <section class="experience" id="resume">
      <div class="container">
        <h2 class="section-title">Professional Experience</h2>
        <div class="experience-list">
          <details class="experience-item">
            <summary class="experience-summary">
              <span class="experience-summary-main">
                <span class="experience-title">AI Product Builder & Founder</span>
                 <div class="experience-company">
                <a
                  href="https://www.butterflow.co/"
                  target="_blank"
                  rel="noopener"
                  >ButterFlow</a
                >
              </div>
              </span>
              <span class="experience-period">2025 - Present</span>
              <span class="experience-indicator" aria-hidden="true"></span>
            </summary>
            <div class="experience-body">
              <ul class="experience-description">
                <li>
                  Designed, built, and shipped production AI systems including agentic workflows and LLM-powered applications — from ideation through delivery.
                </li>
                <li>
                  Built RedFlag, a consumer-facing AI-powered fraud detection tool using an AI fraud agent to analyze screenshots, URLs, and QR codes through security tools to protect users from scams and social engineering.
                </li>
                <li>
                  Developed SHIRO, an agentic AI assistant using Claude Agent SDK and MCP that autonomously prioritizes tasks and executes research — architecting AI agents with memory, tool use, and dynamic skill generation.
                </li>
              </ul>
            </div>
          </details>
          <details class="experience-item">
            <summary class="experience-summary">
              <span class="experience-summary-main">
                <span class="experience-title">Senior Data Analyst | Risk Domain Lead (KYC)</span>
                 <div class="experience-company">
                <a
                  href="https://www.binance.com/"
                  target="_blank"
                  rel="noopener"
                  >Binance</a
                >
              </div>
              </span>
              <span class="experience-period">2022 - 2024</span>
              <span class="experience-indicator" aria-hidden="true"></span>
            </summary>
            <div class="experience-body">
              <ul class="experience-description">
                <li>
                  Drove end-to-end AI and risk strategy for KYC domain, aligning Product, Engineering, Operations, and Compliance teams to scope and deliver data & AI-driven solutions — doubling risk coverage and improving prevention rates by 15% within one year.
                </li>
                <li>
                  Spearheaded a new fraud domain through strategic analysis, translating ambiguous business signals into a roadmap of automated detection rules deployed within six months, catching 28K fraudulent submissions.
                </li>
                <li>
                  Built analytical infrastructure — real-time monitoring pipelines, centralized databases, and stakeholder dashboards — enabling evidence-based iteration on detection models and measurable improvement in decision quality.
                </li>
                <li>
                  Partnered with Data Science teams to co-design ML-based detection models, defining success metrics and translating complex analytical findings into actionable recommendations for senior leadership.
                </li>
              </ul>
            </div>
          </details>

          <details class="experience-item">
            <summary class="experience-summary">
              <span class="experience-summary-main">
                <span class="experience-title">Senior Data Analyst (Trust & Safety)</span>
                   <div class="experience-company">
                <a href="https://www.gojek.com/" target="_blank" rel="noopener"
                  >Gojek (GoTo)</a
                >
              </div>
              </span>
              <span class="experience-period">2021 - 2022</span>
              <span class="experience-indicator" aria-hidden="true"></span>
            </summary>
            <div class="experience-body">
              <ul class="experience-description">
                <li>
                  Identified operational inefficiencies and risk exposure through strategic workflow analysis, delivering $240K in annual cost savings by mapping processes and recommending risk interventions across operations.
                </li>
                <li>
                  Scoped and delivered fraud detection rules for platform abuse in collaboration with Operations, balancing commercial outcomes with compliance requirements and user experience.
                </li>
                <li>
                  Designed real-time monitoring dashboards for stakeholder teams, establishing data-driven decision frameworks that improved case resolution speed and operational visibility.
                </li>
              </ul>
            </div>
          </details>

          <details class="experience-item">
            <summary class="experience-summary">
              <span class="experience-summary-main">
                <span class="experience-title">Product Data Analyst</span>
                  <div class="experience-company">
                <a href="https://www.atome.sg/" target="_blank" rel="noopener"
                  >Atome</a
                >
              </div>
              </span>
              <span class="experience-period">2021</span>
              <span class="experience-indicator" aria-hidden="true"></span>
            </summary>
            <div class="experience-body">
            
              <ul class="experience-description">
                <li>
                  Analyzed user behavior through event logs to optimize in-app
                  flows and improve conversion rates.
                </li>
                <li>
                  Developed Tableau dashboards for app-related data analysis,
                  including funnel analysis and app performance monitoring.
                </li>
              </ul>
            </div>
          </details>

          <details class="experience-item">
            <summary class="experience-summary">
              <span class="experience-summary-main">
                <span class="experience-title">Data Analyst Consultant</span>
                 <div class="experience-company">
                <a href="https://www.pwc.com/" target="_blank" rel="noopener"
                  >PricewaterhouseCoopers</a
                >
              </div>
              </span>
              <span class="experience-period">2019 - 2021</span>
              <span class="experience-indicator" aria-hidden="true"></span>
            </summary>
            <div class="experience-body">
              <ul class="experience-description">
                <li>
                  Delivered consultative, data-driven solutions to enterprise clients in regulated industries, independently managing full project lifecycle from requirements gathering through stakeholder presentation and deployment.
                </li>
                <li>
                  Built automated analytics systems and executive dashboards, translating complex data insights into strategic recommendations for operations and leadership teams.
                </li>
              </ul>
            </div>
          </details>

          <details class="experience-item">
            <summary class="experience-summary">
              <span class="experience-summary-main">
                <span class="experience-title">Python Teaching Assistant (Part Time)</span>
                 <div class="experience-company">
                <a
                  href="https://www.hackwagon.com/"
                  target="_blank"
                  rel="noopener"
                  >Hackwagon Academy</a
                >
              </div>
              </span>
              <span class="experience-period">2018 - 2021</span>
              <span class="experience-indicator" aria-hidden="true"></span>
            </summary>
            <div class="experience-body">
              <ul class="experience-description">
                <li>
                  Supported lead instructor across Python fundamentals and intermediate courses for cohorts of 30–40 students — answering technical questions, marking assignments, and developing an instinct for breaking down complex concepts for non-technical learners.
                </li>
              </ul>
            </div>
          </details>

          <details class="experience-item">
            <summary class="experience-summary">
              <span class="experience-summary-main">
                <span class="experience-title">Data Analyst Intern (Internal Audit)</span>
                  <div class="experience-company">
                <a href="https://www.lazada.com/" target="_blank" rel="noopener"
                  >Lazada</a
                >
                /
                <a
                  href="https://www.alibabagroup.com/en-US/"
                  target="_blank"
                  rel="noopener"
                  >Alibaba</a
                >
              </div>
              </span>
              <span class="experience-period">2018</span>
              <span class="experience-indicator" aria-hidden="true"></span>
            </summary>
            <div class="experience-body">
              <ul class="experience-description">
                <li>
                  Developed real-time fraud detection dashboards using SQL and Alibaba Cloud tools for anomaly detection among Lazada's Southeast Asia ventures, improving risk monitoring efficiency by cutting risk monitoring time by 30%.
                </li>
                <li>
                  Performed exploratory data analytics on voucher and warehouse transactions to uncover unexpected patterns, enabling the team to better understand transactional risks in each venture.
                </li>
              </ul>
            </div>
          </details>
        </div>
      </div>
    </section>

    <!-- Skills & Technologies -->
    <section class="skills" id="skills">
      <div class="container">
        <h2 class="section-title">Skills & Technologies</h2>
        <div class="skills-grid">
          <div class="skill-category">
            <h3>Development & Engineering</h3>
            <div class="skill-tags">
              <span class="skill-tag">AI Agent Development & LLM Integration</span>
              <span class="skill-tag">Workflow Automation (n8n)</span>
              <span class="skill-tag">Python, JavaScript/TypeScript, SQL</span>
              <span class="skill-tag">API Development & Integration</span>
            </div>
          </div>
          <div class="skill-category">
            <h3>Data Analytics  </h3>
            <div class="skill-tags">
              <span class="skill-tag">ETL Development & Data Pipelines</span>
              <span class="skill-tag">Dashboard Development & Visualization</span>
              <span class="skill-tag">Statistical Analysis & Machine Learning</span>
            </div>
          </div>
          <div class="skill-category">
            <h3>Specialized Skills</h3>
            <div class="skill-tags">
              <span class="skill-tag">AI Orchestration & Integration</span>
              <span class="skill-tag">Product Thinking</span>
              <span class="skill-tag">Fraud Analytics & Risk Framework Design </span>
              <span class="skill-tag">Cross-Functional Stakeholder Management</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Let's Connect -->
    <section class="contact" id="contact">
      <div class="container">
        <h2 class="section-title">Let's Connect</h2>
        <p class="contact-description">
          I'm currently seeking AI orchestrator and product manager roles in Singapore.
          <br />I'd also love to connect if you love building with AI.
        </p>
        <div class="contact-links">
          <a href="mailto:sofiakung.ai@gmail.com" class="contact-link-item">
            <i class="fa-solid fa-envelope contact-icon" aria-hidden="true"></i>
            <span>Email</span>
          </a>
          <a href="https://www.linkedin.com/in/sofiakung/" target="_blank" rel="noopener" class="contact-link-item">
            <i class="fab fa-linkedin contact-icon" aria-hidden="true"></i>
            <span>LinkedIn</span>
          </a>
          <a href="https://github.com/sofiakung" target="_blank" rel="noopener" class="contact-link-item">
            <i class="fab fa-github contact-icon" aria-hidden="true"></i>
            <span>GitHub</span>
          </a>
        </div>
      </div>
    </section>
  </main>
  <script is:inline>
    (function(){
      const typedTextSpan = document.getElementById('typed-text');
      const textArray = ['an AI Orchestrator.', 'an AI Product Builder.','a Data Analyst.'];

      if (typedTextSpan && textArray.length) {
        let textArrayIndex = 0;
        let charIndex = 0;
        const typingDelay = 100;
        const erasingDelay = 50;
        const newTextDelay = 2000;

        function type() {
          if (charIndex < textArray[textArrayIndex].length) {
            typedTextSpan.textContent += textArray[textArrayIndex].charAt(charIndex);
            charIndex++;
            setTimeout(type, typingDelay);
          } else {
            setTimeout(erase, newTextDelay);
          }
        }

        function erase() {
          if (charIndex > 0) {
            typedTextSpan.textContent = textArray[textArrayIndex].substring(0, charIndex - 1);
            charIndex--;
            setTimeout(erase, erasingDelay);
          } else {
            textArrayIndex++;
            if (textArrayIndex >= textArray.length) textArrayIndex = 0;
            setTimeout(type, typingDelay + 100);
          }
        }

        setTimeout(type, newTextDelay);
      }


      // Photo deck (swipe through 4 photos + Again button)
      const deck = document.getElementById('photo-deck');
      const againBtn = document.getElementById('deck-again');
      if (deck) {
        const initialPhotos = Array.from(deck.querySelectorAll('.deck-card img')).map((img) => ({
          src: img.getAttribute('src'),
          alt: img.getAttribute('alt') || 'Photo',
        }));
        let startX=0, startY=0, dx=0, dy=0, dragging=false, topCard=null, activePointerId=null;

        function cards() { return Array.from(deck.querySelectorAll('.deck-card')); }

        function createCard(photo) {
          const card = document.createElement('div');
          card.className = 'deck-card';
          const img = document.createElement('img');
          img.src = photo.src || '';
          img.alt = photo.alt || 'Photo';
          img.loading = 'lazy';
          img.setAttribute('draggable', 'false');
          card.appendChild(img);
          return card;
        }

        function setDeckHeight() {
          const firstImg = deck.querySelector('.deck-card img');
          if (!firstImg) return;
          const h = firstImg.complete ? firstImg.clientHeight : 0;
          if (h) deck.style.height = `${h}px`;
          else firstImg.addEventListener('load', () => { deck.style.height = `${firstImg.clientHeight}px`; }, { once: true });
        }

        function layoutDeck() {
          const cs = cards();
          cs.forEach((c, i) => {
            c.classList.toggle('top', i === 0);
            const angle = i === 0 ? 0 : (i % 2 === 0 ? 3 : -3);
            c.style.transform = `translateY(${i * 8}px) scale(${1 - i * 0.03}) rotate(${angle}deg)`;
            c.style.zIndex = String(1000 - i);
            c.style.transition = 'transform .25s ease, opacity .25s ease';
          });
          setDeckHeight();
          if (againBtn) againBtn.hidden = cs.length !== 0; // show when empty
        }

        function restoreDeck() {
          const button = againBtn && againBtn.parentElement === deck ? againBtn : null;
          deck.querySelectorAll('.deck-card').forEach((card) => card.remove());
          initialPhotos.forEach((photo) => {
            const card = createCard(photo);
            if (button) deck.insertBefore(card, button);
            else deck.appendChild(card);
          });
          layoutDeck();
          dragging = false;
          activePointerId = null;
          dx = dy = 0;
          topCard = null;
          deck.classList.remove('dragging');
        }

        function onPointerDown(e) {
          topCard = deck.querySelector('.deck-card.top');
          if (!topCard) return;
          e.preventDefault();
          dragging = true; dx = 0; dy = 0;
          startX = e.clientX || (e.touches && e.touches[0]?.clientX) || 0;
          startY = e.clientY || (e.touches && e.touches[0]?.clientY) || 0;
          activePointerId = e.pointerId != null ? e.pointerId : null;
          try { if (activePointerId != null && topCard.setPointerCapture) topCard.setPointerCapture(activePointerId); } catch{}
          topCard.style.transition = 'none';
          document.addEventListener('pointermove', onPointerMove, { passive: true });
          document.addEventListener('pointerup', onPointerUp, { once: true });
          document.addEventListener('pointercancel', onPointerUp, { once: true });
        }
        function onPointerMove(e) {
          if (!dragging || !topCard) return;
          const x = e.clientX || (e.touches && e.touches[0]?.clientX) || startX;
          const y = e.clientY || (e.touches && e.touches[0]?.clientY) || startY;
          dx = x - startX; dy = (y - startY) * 0.3;
          const w = deck.clientWidth || (topCard.getBoundingClientRect().width || 300);
          const p = Math.max(-1, Math.min(1, dx / (w * 0.6)));
          const rot = p * 12;
          const opacity = 1 - Math.min(1, Math.abs(p)) * 0.7;
          topCard.style.transform = `translate(${dx}px, ${dy}px) rotate(${rot}deg)`;
          topCard.style.opacity = String(opacity);
        }
        function onPointerUp() {
          if (!topCard) return;
          const currentCard = topCard;
          dragging = false;
          try { if (activePointerId != null && currentCard.releasePointerCapture) currentCard.releasePointerCapture(activePointerId); } catch{}
          const rect = currentCard.getBoundingClientRect();
          const w = deck.clientWidth || rect.width;
          const threshold = Math.min(120, rect.width * 0.25);
          const shouldDismiss = Math.abs(dx) > threshold;
          const spring = 'cubic-bezier(0.175, 0.885, 0.32, 1.275)';
          currentCard.style.transition = `transform .25s ${spring}, opacity .25s ease`;
          if (shouldDismiss) {
            const dir = Math.sign(dx) || 1;
            const y = dy;
            currentCard.style.transform = `translate(${dir * w * 1.2}px, ${y}px) rotate(${dir * 30}deg)`;
            currentCard.style.opacity = '0';
            currentCard.addEventListener('transitionend', () => {
              if (currentCard.parentElement === deck) currentCard.remove();
              layoutDeck();
            }, { once: true });
          } else {
            currentCard.style.transform = 'translate(0,0) rotate(0deg)';
            currentCard.style.opacity = '1';
            currentCard.addEventListener('transitionend', () => {
              currentCard.style.transition = 'none';
            }, { once: true });
          }
          dx = dy = 0; topCard = null; deck.classList.remove('dragging');
          document.removeEventListener('pointermove', onPointerMove);
          document.removeEventListener('pointercancel', onPointerUp);
        }
        deck.querySelectorAll('.deck-card img').forEach((img) => img.setAttribute('draggable', 'false'));
        deck.addEventListener('pointerdown', (e) => {
          if (!(e.target && e.target.closest('.deck-card'))) return;
          deck.classList.add('dragging');
          onPointerDown(e);
        });
        layoutDeck();
        if (againBtn) againBtn.addEventListener('click', restoreDeck);
      }
    })();
  </script>
</BaseLayout>
